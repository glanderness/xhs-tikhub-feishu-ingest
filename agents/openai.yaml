interface:
  display_name: "XHS TikHub Feishu Ingest"
  short_description: "Sync XHS notes into a Feishu Base"
  default_prompt: "Use $xhs-tikhub-feishu-ingest to collect this Xiaohongshu link and append it to the Feishu Base."
policy:
  allow_implicit_invocation: true
